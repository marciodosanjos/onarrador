/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["onarrador.com"], // Add the hostname here
  },
  //basePath: "/app", // Define o subdiretório onde o Next.js será hospedado
  // Outras configurações do Next.js podem ser adicionadas aqui, se necessário
};

module.exports = nextConfig;
